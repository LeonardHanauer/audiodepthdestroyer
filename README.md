A simple Rust tool for quantizing WAV audio down to 1-bit depth, built on `hound`.

- Reads a WAV file and hard-quantizes each sample to 1 bit quality
- Outputs a new WAV file with a random filename, saved alongside the source
- Prebuilt Windows binary

Built as a quick tinkering project — no guarantees, use at your own risk.
